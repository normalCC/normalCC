<!-- <div class="modal fade bs-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel" aria-hidden="true"> -->
  <script type="text/javascript">
    $(".modal").on("click", function(){
      // console.log('modal was clicked! by user: <%= current_user.name %>');
      document.getElementById('skip-button').click();
    });
  </script>
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div id='chart_holder'><%# render "graph" goes here ! %> </div>
    </div>
  </div>
</div>

<div class="row white_Playfair text-center">
  <h2><%= @question.title %></h2>
  <!-- <p>Created on: <%= stdDate(@question.created_at) %></p> 
  <p>Created by: <%#= @question.user_email %></p>
  <p>View count: <%#= @question.view_count %></p>
  <p>Like count: <%#= @question.likes_count %></p> -->
  <%# current_user.list_of_remaining_questions.first.id %>
<div class="row">
  <div class="col-xs-2 col-sm-3 col-md-4"></div>
  <div class="col-xs-8 col-sm-6 col-md-4">   
    <!-- Show answers here -->
    <% @question.answers.each do |a| %>
      <%= button_to a.title, scorecards_path(a), method: :post, remote: true,
        class: "btn btn-info btn-wide", data: { toggle: "modal", target: ".bs-example-modal-lg" } %>
      <br>
    <% end %>
  </div>
</div>
  <%= link_to 'Skip this question', question_path(@next_question), { :id => "skip-button"} %>

   <!-- use with miniMagick image attachment -->
  <%#= image_tag @question.image_url(:medium) %>

  <!-- <p>Like count: <%#= @question.likes_count %></p> -->

  <% if @question.user == current_user  %>
  <%# if can? :edit, @campaign %>
    <br>
    <%= link_to "Edit your question", edit_question_path(@question), class: "btn btn-info" %>
  <%# end %>
  <%# if can? :destroy, @campaign %>
    <%= link_to "Delete your question", @question, class: "btn btn-danger", method: :delete, data: {confirm: "Are you sure to delete?"} %>
  <%# else %>
  <% end %>
  
</div>
