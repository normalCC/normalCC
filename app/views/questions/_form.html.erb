
<h1>Create a question</h1>

<%= simple_form_for @question, html: { class: 'form-horizontal' } do |f| %>
  <%= f.input :title, label: "", :input_html => {:size => 80, :maxlength => 80} %> 

  <h3>Edit Answers:</h3>
  <p> It's good to give a wide spectrum of answers for people to pick. It'll make your graph look lovely.</p>
  <%= f.full_error :answers %>
  <%= f.simple_fields_for :answers do |ans| %>
    <%= render partial: "answer_fields", locals: { f: ans } %>
  <% end %>

  <div class="links">
    <%= link_to_add_association 'Add more answer choices', f, :answers, class: "btn btn-primary" %>
  </div>
 <br>

  <%= f.submit class: "btn btn-lg" %>
  <br>
  <br>
  <br>
 
<% end %>
